<ContentDialog
    x:Class="iSee.RegisterContentDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:iSee"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    PrimaryButtonText="注册"  
    SecondaryButtonText="取消"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    SecondaryButtonClick="ContentDialog_SecondaryButtonClick" MaxHeight="300" FullSizeDesired="True" Background="White" Height="357.66">

    
    <StackPanel x:Name="registerStackPanel" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Width="300">
        <TextBlock Text="注册账号" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,10" FontSize="20"></TextBlock>
        <TextBox x:Name="userNameTextBox" PlaceholderText="用户名" Margin="0,0,0,20" Background="{x:Null}" Style="{StaticResource TextInput}"/>
        <PasswordBox x:Name="passwordTextBox" IsPasswordRevealButtonEnabled="True" PlaceholderText="密码" Margin="0,0,0,20" Style="{StaticResource PasswordInput}"/>
        <PasswordBox x:Name="repasswordTextBox" IsPasswordRevealButtonEnabled="True" PlaceholderText="重复密码" Margin="0,0,0,10" Style="{StaticResource PasswordInput}"/>
        <TextBlock x:Name="errorTextBlock" />
    </StackPanel>
</ContentDialog>